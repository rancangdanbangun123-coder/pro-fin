import React, { useEffect, useState } from 'react';

export default function ProcurementDetailModal({ isOpen, onClose, item, onUpdateItem, onEditClick, onViewHistory }) {
    const [isVisible, setIsVisible] = useState(false);
    const [animateIn, setAnimateIn] = useState(false);

    useEffect(() => {
        if (isOpen) {
            setIsVisible(true);
            setTimeout(() => setAnimateIn(true), 10);
        } else {
            setAnimateIn(false);
            const timer = setTimeout(() => setIsVisible(false), 300);
            return () => clearTimeout(timer);
        }
    }, [isOpen]);

    if (!isVisible || !item) return null;

    // Helper to determine status color
    const getStatusColor = (stage) => {
        switch (stage) {
            case 'pr': return 'bg-slate-500';
            case 'rfq': return 'bg-blue-500';
            case 'selection': return 'bg-purple-500';
            case 'po': return 'bg-primary';
            case 'invoice': return 'bg-orange-500';
            case 'do': return 'bg-teal-500';
            case 'done': return 'bg-green-500';
            default: return 'bg-slate-500';
        }
    };

    const timelineSteps = [
        { id: 'pr', label: 'PR (Permintaan)', date: '20 Oct' },
        { id: 'po', label: 'PO (Pesanan)', date: '24 Oct' },
        { id: 'invoice', label: 'Invoice', date: '-' },
        { id: 'do', label: 'DO (Diterima)', date: 'Pending' },
        { id: 'evaluation', label: 'Evaluasi', date: '-' },
        { id: 'done', label: 'Selesai', date: '-' },
    ];

    const currentStepIndex = timelineSteps.findIndex(step => step.id === item.stage);

    return (
        <div className={`fixed inset-0 z-50 flex items-center justify-center p-4 sm:p-6 transition-opacity duration-300 ${animateIn ? 'opacity-100' : 'opacity-0'}`}>
            {/* Backdrop */}
            <div
                className="absolute inset-0 bg-slate-900/40 dark:bg-black/60 backdrop-blur-sm transition-opacity"
                onClick={onClose}
            ></div>

            {/* Modal Content */}
            <div
                className={`relative w-full max-w-2xl bg-white dark:bg-card-dark rounded-2xl shadow-2xl overflow-hidden flex flex-col max-h-[90vh] transition-all duration-300 transform ${animateIn ? 'scale-100 translate-y-0' : 'scale-95 translate-y-4'}`}
            >
                {/* Header */}
                <div className="flex items-center justify-between px-6 py-4 border-b border-slate-100 dark:border-border-dark bg-slate-50/50 dark:bg-surface-dark/50">
                    <div className="flex items-center gap-3">
                        <span className="text-sm font-mono font-bold text-slate-500 bg-slate-200 dark:bg-surface-dark-lighter px-2 py-1 rounded">{item.code}</span>
                        <span className={`h-2 w-2 rounded-full ${getStatusColor(item.stage)}`}></span>
                        <span className="text-sm font-medium text-slate-600 dark:text-slate-300 uppercase tracking-wide">{item.stage}</span>
                    </div>
                    <button
                        onClick={onClose}
                        className="text-slate-400 hover:text-slate-600 dark:hover:text-slate-200 transition-colors p-1 rounded-full hover:bg-slate-200/50 dark:hover:bg-white/5"
                    >
                        <span className="material-icons-round">close</span>
                    </button>
                </div>

                {/* Scrollable Body */}
                <div className="overflow-y-auto p-6 custom-scrollbar">

                    {/* Title & Meta */}
                    <div className="mb-8">
                        <div className="flex items-start justify-between gap-4 mb-2">
                            <h2 className="text-2xl font-bold text-slate-900 dark:text-white leading-tight">{item.title}</h2>
                            {item.urgent && (
                                <span className="shrink-0 flex items-center gap-1 text-xs font-bold text-orange-600 bg-orange-100 dark:bg-orange-500/20 dark:text-orange-400 px-2 py-1 rounded-full uppercase tracking-wide">
                                    <span className="material-icons-round text-sm">warning</span> Urgent
                                </span>
                            )}
                        </div>
                        <div className="flex flex-wrap gap-2 text-sm text-slate-500 dark:text-slate-400">
                            <span className="flex items-center gap-1 bg-slate-100 dark:bg-surface-dark-lighter px-2 py-1 rounded">
                                <span className="material-icons-round text-sm">apartment</span> {item.project}
                            </span>
                            <span className="flex items-center gap-1 bg-slate-100 dark:bg-surface-dark-lighter px-2 py-1 rounded">
                                <span className="material-icons-round text-sm">category</span> {item.type}
                            </span>
                        </div>
                    </div>

                    {/* Timeline */}
                    <div className="mb-8 overflow-x-auto pb-4">
                        <div className="flex items-center min-w-max">
                            {timelineSteps.map((step, index) => {
                                const isCompleted = index <= currentStepIndex;
                                const isCurrent = index === currentStepIndex;

                                return (
                                    <div key={step.id} className="flex items-center relative">
                                        <div className="flex flex-col items-center gap-2 w-24 text-center z-10">
                                            <div className={`w-8 h-8 rounded-full flex items-center justify-center border-2 transition-colors ${isCompleted ? 'bg-primary border-primary text-white' : 'bg-white dark:bg-card-dark border-slate-300 dark:border-slate-600 text-slate-300'}`}>
                                                {isCompleted ? <span className="material-icons-round text-sm">check</span> : <span className="text-xs font-medium">{index + 1}</span>}
                                            </div>
                                            <div>
                                                <p className={`text-[10px] font-bold uppercase tracking-wider ${isCurrent ? 'text-primary' : (isCompleted ? 'text-slate-700 dark:text-slate-300' : 'text-slate-400')}`}>{step.label}</p>
                                                <p className="text-[10px] text-slate-400">{step.date}</p>
                                            </div>
                                        </div>
                                        {index < timelineSteps.length - 1 && (
                                            <div className={`h-0.5 w-16 -mx-4 mb-6 transition-colors ${index < currentStepIndex ? 'bg-primary' : 'bg-slate-200 dark:bg-slate-700'}`}></div>
                                        )}
                                    </div>
                                );
                            })}
                        </div>
                    </div>

                    {/* Details Conditional Blocks */}
                    {item.stage === 'pr' && (
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Informasi Kebutuhan</h3>
                                <div className="grid grid-cols-2 gap-4">
                                    <div>
                                        <p className="text-xs text-slate-500 mb-1">Volume Request</p>
                                        <p className="text-sm font-medium text-slate-900 dark:text-white">{item.qty || item.vol || '-'}</p>
                                    </div>
                                    <div>
                                        <p className="text-xs text-slate-500 mb-1">Estimasi Anggaran</p>
                                        <p className="text-sm font-medium text-slate-900 dark:text-white">{item.est?.replace('Est: ', '') || '-'}</p>
                                    </div>
                                </div>
                            </div>

                            {/* Dynamic Combined Items Render (Only visible in PR usually) */}
                            {item.rawItems && Array.isArray(item.rawItems) && item.rawItems.length > 0 && (
                                <div className="col-span-1 md:col-span-2 mt-2">
                                    <h4 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2 mb-3">Rincian Item Gabungan</h4>
                                    <div className="overflow-x-auto rounded-lg border border-slate-200 dark:border-border-dark">
                                        <table className="min-w-full divide-y divide-slate-200 dark:divide-border-dark text-left text-sm">
                                            <thead className="bg-slate-50 dark:bg-surface-dark-lighter">
                                                <tr>
                                                    <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300">Nama Item</th>
                                                    <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300 text-center">Qty</th>
                                                    <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300 text-right">Harga Satuan</th>
                                                    <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300 text-right">Subtotal</th>
                                                </tr>
                                            </thead>
                                            <tbody className="divide-y divide-slate-100 dark:divide-border-dark/50 bg-white dark:bg-card-dark">
                                                {item.rawItems.map((rawItem, idx) => (
                                                    <tr key={idx} className="hover:bg-slate-50 dark:hover:bg-surface-dark-lighter/30 transition-colors">
                                                        <td className="px-3 py-2 whitespace-nowrap text-slate-700 dark:text-slate-300 font-medium">
                                                            {rawItem.name}
                                                            <div className="text-[10px] text-slate-400 font-normal">{rawItem.category || '-'}</div>
                                                        </td>
                                                        <td className="px-3 py-2 whitespace-nowrap text-center text-slate-600 dark:text-slate-400">
                                                            {rawItem.qty} {rawItem.unit}
                                                        </td>
                                                        <td className="px-3 py-2 whitespace-nowrap text-right text-slate-600 dark:text-slate-400">
                                                            Rp {(rawItem.price || 0).toLocaleString('id-ID')}
                                                        </td>
                                                        <td className="px-3 py-2 whitespace-nowrap text-right font-semibold text-slate-800 dark:text-slate-200">
                                                            Rp {((rawItem.qty || 1) * (rawItem.price || 0)).toLocaleString('id-ID')}
                                                        </td>
                                                    </tr>
                                                ))}
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            )}
                        </div>
                    )}



                    {item.stage === 'po' && (
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Purchase Order (PO)</h3>
                                <div className="grid grid-cols-2 gap-4">
                                    <div className="col-span-2 bg-primary/5 dark:bg-primary/10 p-4 rounded-xl border border-primary/20 flex items-center gap-4 shadow-sm">
                                        <div className="w-12 h-12 rounded-full bg-white dark:bg-card-dark flex items-center justify-center text-primary shadow-sm border border-primary/10">
                                            <span className="material-icons-round text-2xl">store</span>
                                        </div>
                                        <div>
                                            <p className="text-xs text-slate-500 mb-0.5">Supplier / Vendor</p>
                                            <p className="text-base font-bold text-slate-900 dark:text-white">{item.store || '-'}</p>
                                        </div>
                                    </div>
                                    <div className="bg-slate-50 dark:bg-surface-dark-lighter p-3 rounded-lg border border-slate-200 dark:border-border-dark flex flex-col justify-center">
                                        <p className="text-xs text-slate-500 mb-1">Total Nilai PO</p>
                                        <p className="text-lg font-bold text-primary">{item.total || item.est || 'Rp 0'}</p>
                                    </div>
                                    <div className="bg-slate-50 dark:bg-surface-dark-lighter p-3 rounded-lg border border-slate-200 dark:border-border-dark flex flex-col justify-center">
                                        <p className="text-xs text-slate-500 mb-1 flex items-center gap-1"><span className="material-icons-round text-sm">local_shipping</span> Max ETA</p>
                                        <p className="text-sm font-bold text-slate-700 dark:text-slate-300">{item.eta || 'TBD'}</p>
                                    </div>
                                </div>
                            </div>
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Informasi Barang</h3>
                                <div className="grid grid-cols-2 gap-4">
                                    <div>
                                        <p className="text-xs text-slate-500 mb-1">Jumlah</p>
                                        <p className="text-sm font-medium text-slate-900 dark:text-white">{item.qty || item.vol || '-'}</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    )}

                    {item.stage === 'invoice' && (() => {
                        const bills = item.bills || [];
                        const hasBills = bills.length > 0;
                        const paidCount = hasBills ? bills.filter(b => b.status === 'Lunas').length : 0;
                        const paidPercent = hasBills ? Math.round((paidCount / bills.length) * 100) : 0;

                        return (
                            <div className="grid grid-cols-1 gap-6 mb-8">
                                <div className="space-y-4">
                                    <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Status Penagihan</h3>

                                    {/* Summary Card */}
                                    <div className="bg-orange-50 dark:bg-orange-900/10 p-5 rounded-xl border border-orange-200 dark:border-orange-800/40 shadow-sm">
                                        <div className="flex items-center justify-between mb-3">
                                            <p className="text-xs text-slate-500 uppercase tracking-wider">Total Tagihan</p>
                                            <span className={`px-2 py-0.5 rounded text-xs font-bold border ${paidCount === bills.length && hasBills ? 'bg-green-100 text-green-700 border-green-200 dark:bg-green-900/30 dark:text-green-400 dark:border-green-800' :
                                                    paidCount > 0 ? 'bg-orange-100 text-orange-700 border-orange-200 dark:bg-orange-900/30 dark:text-orange-400 dark:border-orange-800' :
                                                        'bg-red-100 text-red-700 border-red-200 dark:bg-red-900/30 dark:text-red-400 dark:border-red-800'
                                                }`}>
                                                {paidCount === bills.length && hasBills ? 'Lunas' : paidCount > 0 ? 'Sebagian' : 'Unpaid'}
                                            </span>
                                        </div>
                                        <p className="text-3xl font-black text-orange-600 dark:text-orange-400 mb-3">{item.total || 'Rp 0'}</p>
                                        {hasBills && (
                                            <div className="space-y-2">
                                                <div className="flex items-center justify-between text-xs text-slate-500">
                                                    <span>{paidCount} dari {bills.length} tagihan lunas</span>
                                                    <span className="font-bold">{paidPercent}%</span>
                                                </div>
                                                <div className="w-full bg-slate-200 dark:bg-slate-700 rounded-full h-2 overflow-hidden">
                                                    <div className="bg-green-500 h-full rounded-full transition-all duration-500" style={{ width: `${paidPercent}%` }}></div>
                                                </div>
                                            </div>
                                        )}
                                    </div>

                                    {/* Bills Table */}
                                    {hasBills && (
                                        <div className="overflow-x-auto rounded-lg border border-slate-200 dark:border-border-dark">
                                            <table className="min-w-full divide-y divide-slate-200 dark:divide-border-dark text-left text-sm">
                                                <thead className="bg-slate-50 dark:bg-surface-dark-lighter">
                                                    <tr>
                                                        <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300">Tagihan</th>
                                                        <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300 text-right">Nilai</th>
                                                        <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300">Jatuh Tempo</th>
                                                        <th className="px-3 py-2 text-xs font-semibold text-slate-600 dark:text-slate-300 text-center">Status</th>
                                                    </tr>
                                                </thead>
                                                <tbody className="divide-y divide-slate-100 dark:divide-border-dark/50 bg-white dark:bg-card-dark">
                                                    {bills.map((bill) => (
                                                        <tr key={bill.id} className="hover:bg-slate-50 dark:hover:bg-surface-dark-lighter/30 transition-colors">
                                                            <td className="px-3 py-2.5 whitespace-nowrap">
                                                                <div className="flex items-center gap-2">
                                                                    <span className={`material-icons-round text-[14px] ${bill.status === 'Lunas' ? 'text-green-500' : 'text-orange-400'}`}>
                                                                        {bill.status === 'Lunas' ? 'check_circle' : 'pending'}
                                                                    </span>
                                                                    <span className="font-medium text-slate-700 dark:text-slate-300">{bill.label}</span>
                                                                </div>
                                                            </td>
                                                            <td className="px-3 py-2.5 whitespace-nowrap text-right font-semibold text-slate-800 dark:text-slate-200">{bill.amount}</td>
                                                            <td className="px-3 py-2.5 whitespace-nowrap text-slate-600 dark:text-slate-400">{bill.due || '-'}</td>
                                                            <td className="px-3 py-2.5 whitespace-nowrap text-center">
                                                                <span className={`px-2 py-0.5 rounded-full text-[10px] font-bold uppercase ${bill.status === 'Lunas' ? 'bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400' :
                                                                        bill.status === 'Dibayar Sebagian' ? 'bg-yellow-100 text-yellow-700 dark:bg-yellow-900/30 dark:text-yellow-400' :
                                                                            'bg-red-100 text-red-600 dark:bg-red-900/30 dark:text-red-400'
                                                                    }`}>
                                                                    {bill.status}
                                                                </span>
                                                            </td>
                                                        </tr>
                                                    ))}
                                                </tbody>
                                            </table>
                                        </div>
                                    )}

                                    {/* Fallback for items without bills[] */}
                                    {!hasBills && (
                                        <div className="bg-white dark:bg-card-dark rounded-lg divide-x divide-slate-100 dark:divide-slate-700 border border-slate-200 dark:border-slate-700 overflow-hidden shadow-sm flex">
                                            <div className="flex-1 py-2 px-3 text-left">
                                                <p className="text-[10px] text-slate-400 mb-0.5">Status</p>
                                                <p className={`text-xs font-bold ${item.status === 'Unpaid' ? 'text-red-500' : 'text-slate-700 dark:text-slate-300'}`}>{item.status || 'Unpaid'}</p>
                                            </div>
                                            <div className="flex-1 py-2 px-3 text-left">
                                                <p className="text-[10px] text-slate-400 mb-0.5">Jatuh Tempo</p>
                                                <p className="text-xs font-bold text-slate-700 dark:text-slate-300">{item.due || 'TBD'}</p>
                                            </div>
                                        </div>
                                    )}
                                </div>
                            </div>
                        );
                    })()}

                    {item.stage === 'do' && (
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Penerimaan Lapangan</h3>
                                <div className="bg-slate-50 dark:bg-surface-dark-lighter p-4 rounded-xl border border-slate-200 dark:border-border-dark">
                                    <p className="text-xs text-slate-500 mb-1">Diterima Oleh (Receiver)</p>
                                    <p className="text-base font-bold text-slate-900 dark:text-white mb-4">{item.recv || 'Menunggu Kedatangan'}</p>

                                    {item.checklist && (
                                        <div className="space-y-3 pt-4 border-t border-slate-200 dark:border-slate-700">
                                            <div className="flex items-center gap-3">
                                                <span className={`material-icons-round ${item.checklist.physical ? 'text-teal-500' : 'text-slate-300 dark:text-slate-600'}`}>{item.checklist.physical ? 'check_circle' : 'radio_button_unchecked'}</span>
                                                <span className={`text-sm ${item.checklist.physical ? 'text-teal-700 dark:text-teal-400 font-medium' : 'text-slate-500'}`}>Cek Kualitas & Kuantitas Fisik</span>
                                            </div>
                                            <div className="flex items-center gap-3">
                                                <span className={`material-icons-round ${item.checklist.doc ? 'text-teal-500' : 'text-slate-300 dark:text-slate-600'}`}>{item.checklist.doc ? 'check_circle' : 'radio_button_unchecked'}</span>
                                                <span className={`text-sm ${item.checklist.doc ? 'text-teal-700 dark:text-teal-400 font-medium' : 'text-slate-500'}`}>Validasi Surat Jalan DO</span>
                                            </div>
                                        </div>
                                    )}
                                </div>
                            </div>
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Informasi Barang</h3>
                                <div className="grid grid-cols-2 gap-4">
                                    <div>
                                        <p className="text-xs text-slate-500 mb-1">Jumlah Datang</p>
                                        <p className="text-sm font-medium text-slate-900 dark:text-white">{item.qty || item.vol || '-'}</p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    )}

                    {item.stage === 'evaluation' && (
                        <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                            <div className="space-y-4">
                                <h3 className="text-sm font-bold text-slate-900 dark:text-white uppercase tracking-wider border-b border-slate-200 dark:border-border-dark pb-2">Evaluasi Vendor</h3>
                                <div className="bg-yellow-50 dark:bg-yellow-900/10 p-6 rounded-xl border border-yellow-200 dark:border-yellow-800/40 text-center shadow-sm flex flex-col justify-center items-center">
                                    <p className="text-xs font-bold text-slate-600 dark:text-slate-400 mb-3">{item.statusLabel || 'Menunggu Penilaian'}</p>
                                    <div className="flex text-yellow-500 text-3xl">
                                        <span className="material-icons-round">star</span>
                                        <span className="material-icons-round">star</span>
                                        <span className="material-icons-round">star</span>
                                        <span className="material-icons-round">star</span>
                                        <span className="material-icons-round opacity-40">star_border</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    )}

                    {item.stage === 'done' && (
                        <div className="grid grid-cols-1 mb-8">
                            <div className="bg-green-50 dark:bg-green-900/10 border border-green-200 dark:border-green-800/40 rounded-xl p-6 shadow-sm flex flex-col md:flex-row items-center justify-between gap-6">
                                <div className="flex items-center gap-4">
                                    <div className="w-16 h-16 rounded-full bg-green-500 flex items-center justify-center text-white shadow-md">
                                        <span className="material-icons-round text-3xl">task_alt</span>
                                    </div>
                                    <div>
                                        <h3 className="text-xl font-bold text-green-800 dark:text-green-400">Pengadaan Selesai</h3>
                                        <p className="text-sm text-green-700/80 dark:text-green-300/80">Barang diterima dan tagihan lunas.</p>
                                    </div>
                                </div>
                                <div className="bg-white dark:bg-card-dark p-4 rounded-lg border border-green-100 dark:border-green-800/40 text-center w-full md:w-auto min-w-[200px]">
                                    <p className="text-xs text-slate-500 mb-1 uppercase tracking-wider">Total Nilai Akhir</p>
                                    <p className="text-2xl font-black text-slate-900 dark:text-white">{item.total || item.est || 'Rp 0'}</p>
                                </div>
                            </div>
                        </div>
                    )}

                </div>

                {/* Footer Actions */}
                <div className="p-4 border-t border-slate-200 dark:border-border-dark bg-slate-50 dark:bg-surface-dark flex justify-end gap-3">
                    <button
                        onClick={() => onViewHistory && onViewHistory(item)}
                        className="px-4 py-2 text-sm font-medium text-slate-600 dark:text-slate-300 hover:text-primary transition-colors flex items-center gap-1.5"
                    >
                        <span className="material-icons-round text-[16px]">history</span>
                        Riwayat
                    </button>
                    <button
                        onClick={onClose}
                        className="px-4 py-2 text-sm font-medium text-slate-600 dark:text-slate-300 hover:text-slate-900 dark:hover:text-white transition-colors"
                    >
                        Close
                    </button>
                    <button
                        onClick={() => onEditClick(item)}
                        className="flex items-center gap-2 px-4 py-2 bg-primary hover:bg-primary-hover text-white text-sm font-medium rounded-lg shadow-lg shadow-primary/20 transition-all transform hover:scale-105 active:scale-95"
                    >
                        <span className="material-icons-round text-base">edit</span>
                        Edit Item
                    </button>
                </div>
            </div>
        </div>
    );
}
